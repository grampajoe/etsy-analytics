machine:
  node:
    version: iojs-v1.3.0

general:
  branches:
    only:
      - master

deployment:
  production:
    branch: master
    commands:
      - npm run-script build
      - npm run-script deploy
